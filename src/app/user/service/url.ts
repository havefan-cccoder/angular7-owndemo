import { host } from './HOST';

export const Urls = {
     login: host + `/login`,
     getWeidu: host + `/lightModel/baseInformation/getWeidu`,
     getResourcePoolList: host + `/lightModel/resourcePool/getResourcePoolList`,
     getModelList: host + `/lightModel/model/get/modelList`,
     getMyResource: host + `/lightModel/resourcePool/find/myResource`,
};